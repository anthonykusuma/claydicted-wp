{% if main_menu %}
	{% for item in items %}
		<a href="{{ item.link }}" target="{{ item.target }}" class="-mx-3 block rounded-lg px-3 py-2 text-base font-semibold leading-7 text-gray-900 hover:bg-gray-50">{{ item.title }}</a>
	{% endfor %}
{% endif %}
